#include <stdio.h>
/**
 * main - Entry point
 *
 * @argc: stores the number or arguments passed
 * @argv: stores the arguments passed in an array
 *
 * Return: Always 0 (Success)
 */
int main(int argc, char __attribute__((unused)) *argv[])
{
	printf("%d\n", argc - 1);

	return (0);
}
